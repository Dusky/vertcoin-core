# Paperclips Docker Configuration

# Network to use (mainnet, testnet, signet, regtest)
NETWORK=mainnet

# RPC settings for external services
RPC_USER=paperclipsrpc
RPC_PASSWORD=changeme
RPC_PORT=5888

# Port mapping (host:container)
P2P_PORT=5889:5889

# Data directory location on host
HOST_DATA_DIR=/data/paperclips

# External IP address (important for seed nodes)
EXTERNAL_IP=

# Optional: For seed nodes configuration
MAX_CONNECTIONS=256

# Optional: For testnet/regtest configuration
TESTNET_PORT=15889:15889